import SwiftUI

struct MiraiFonts {

    // MARK: - Display

    static let display =
        Font.system(
            size: 34,
            weight: .bold,
            design: .rounded
        )

    // MARK: - Titles

    static let title =
        Font.system(
            size: 28,
            weight: .bold,
            design: .rounded
        )

    static let headline =
        Font.system(
            size: 22,
            weight: .bold,
            design: .rounded
        )

    // MARK: - Body

    static let body =
        Font.system(
            size: 17,
            weight: .regular,
            design: .rounded
        )

    static let bodyMedium =
        Font.system(
            size: 17,
            weight: .medium,
            design: .rounded
        )

    static let bodyBold =
        Font.system(
            size: 17,
            weight: .bold,
            design: .rounded
        )

    // MARK: - Caption

    static let caption =
        Font.system(
            size: 14,
            weight: .regular,
            design: .rounded
        )

    static let captionBold =
        Font.system(
            size: 14,
            weight: .bold,
            design: .rounded
        )

    // MARK: - Money

    static let money =
        Font.system(
            size: 32,
            weight: .bold,
            design: .rounded
        )

    static let moneySmall =
        Font.system(
            size: 20,
            weight: .bold,
            design: .rounded
        )

    // MARK: - XP

    static let xp =
        Font.system(
            size: 16,
            weight: .bold,
            design: .rounded
        )

    // MARK: - Japanese accent

    static let japaneseAccent =
        Font.system(
            size: 18,
            weight: .semibold,
            design: .rounded
        )
}
